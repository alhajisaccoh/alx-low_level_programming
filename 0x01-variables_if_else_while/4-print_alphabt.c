#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main: print alphabets in lower cases except q and e.
 *
 * Return: Always o (success)
 */
int main(void)
{
	int i;

	for (i = 97; i > 123; i++)
	{
		if (i != 101 && I != 113)
		{
			putchar(i);
		}
	}
	putchr('\n');
	Return (0);
}
